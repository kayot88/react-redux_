import React from "react";
import spinner from "../../img/logo.svg";

const Spinner = () => {
  return (
    <div>
      <img src={spinner} alt="spinner" />
    </div>
  );
};

export default Spinner;
